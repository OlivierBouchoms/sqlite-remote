# {{os}} {{version}} mock server

## SSH config

Copy the generated SSH key to the Docker container. The password for the root account is `mock`.

```shell
ssh-copy-id -i ~/.ssh/id_remote_sqlite -p {{port}} root@localhost
```

Add the following entry to your SSH config file (`~/.ssh/config`)

```
Host sqlite_{{os_snake_case}}_{{version_snake_case}}
    HostName 127.0.0.1
    User root
    Port {{port}}
    IdentityFile ~/.ssh/id_remote_sqlite
```

## App database configuration

- Label: {{os}} {{version}} mock server
- Hostname: sqlite_{{os_snake_case}}_22_04
- Database path: /db/sqlite.db
